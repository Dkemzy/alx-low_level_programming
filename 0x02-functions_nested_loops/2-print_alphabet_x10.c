#include "main.h"
/**
 * main-Entrypoint
 * main.h contains functions needed for these tasks
 * Return: Always 0(Success)
 */
int main(void)
{
	int alphabet_count = 0
	{
		char m='a';

		while (m <= 'z')
		{
			_putchar(m);
			m++;
		}
		alphabet_count++;
		_putchar('\n')
	}
}
